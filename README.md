# apps-portal

GitHub で公開しているアプリ・ツールの一覧ページ。

## 追加方法

`index.html` 内のカードブロックをコピーして、名前・説明・GitHub URL を書き換えるだけで追加できます。

## GitHub Pages

`Settings > Pages` で `main` ブランチのルートを公開元に設定することで、そのまま Web 公開できます。
